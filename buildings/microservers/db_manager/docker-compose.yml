version: '3.8'

services:
  db-manager:
    build: .
    container_name: db-manager
    restart: always
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
    ports:
      - "8000:8000"